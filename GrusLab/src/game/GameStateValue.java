package game;

/**
 * @author lilpr
 * This enum holds the states which the game itself can have.
 */
public enum GameStateValue {
	INIT,
	CALIBRATION,
	READY,
	COUNTDOWN,
	PLAY,
	PAUSE,
	FINISHED,
	EXIT;
}
